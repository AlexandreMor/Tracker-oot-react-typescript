import React from 'react'
import { Locations } from '../../stores/locationsState';

type Props = {
    locations: Locations;
  };

export const AlwaysHints = ({locations} : Props) => {
  return (
    <div>
        <h3></h3>
    </div>
  )
}
